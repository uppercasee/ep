import { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // webpack: (config) => {
  //   config.experiments = {
  //     topLevelAwait: true,
  //   }
  //   return config
  // },
}

export default nextConfig
